import React from 'react'

export default function Project({img, name, desc}){
  return (
    <div className="project">
      <img src="" alt="" />
      <h4>{name}</h4>
      <p>{desc}</p>
    </div>
  )
}